# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: gw/ping/ping-api-error.spec.ts >> gw ping api - errors >> validate a revoked key is rejected on ping
- Location: tests/functional/gw/ping/ping-api-error.spec.ts:25:7

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 401
Received: 200
```

```
Error: expect(received).toEqual(expected) // deep equality

- Expected  - 3
+ Received  + 3

  Object {
-   "error": "Unauthorized",
-   "message": "Invalid API key",
-   "statusCode": 401,
+   "at": "2026-09-24T08:12:49.526Z",
+   "pong": true,
+   "tenant": "1479e6f9-1c49-410a-b921-e6e151aa780d",
  }
```

# Test source

```ts
  1  | import { APIRequestContext, APIResponse, expect, PlaywrightWorkerArgs } from '@playwright/test';
  2  | import { environment } from '../config/environments';
  3  | 
  4  | export type HttpMethod = 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';
  5  | 
  6  | export interface ApiRequestParams {
  7  |   method: HttpMethod;
  8  |   url: string;
  9  |   headers?: Record<string, string>;
  10 |   body?: object;
  11 | }
  12 | 
  13 | export async function sendApiRequest(request: APIRequestContext, params: ApiRequestParams): Promise<APIResponse> {
  14 |   const { method, url, headers, body } = params;
  15 |   return request.fetch(url, { method, headers, data: body });
  16 | }
  17 | 
  18 | // beforeAll/afterAll run outside test scope, so the test-scoped `request` fixture isn't
  19 | // available there. This mints a throwaway APIRequestContext for the duration of `fn` and
  20 | // disposes it after, so any flow function (login, createTenant, revokeKey, ...) can run
  21 | // in a hook without a bespoke *InHook variant of its own.
  22 | export async function withHookRequestContext<T>(
  23 |   playwright: PlaywrightWorkerArgs['playwright'],
  24 |   fn: (request: APIRequestContext) => Promise<T>,
  25 | ): Promise<T> {
  26 |   const context = await playwright.request.newContext({ baseURL: environment.apiBaseUrl });
  27 |   try {
  28 |     return await fn(context);
  29 |   } finally {
  30 |     await context.dispose();
  31 |   }
  32 | }
  33 | 
  34 | export function assertResponseStatus(response: APIResponse, expectedStatus: number) {
  35 |   expect.soft(response.status()).toBe(expectedStatus);
  36 | }
  37 | 
  38 | export function assertResponseBody(actual: unknown, expected: Record<string, unknown> | unknown[], options?: { exact?: boolean }) {
  39 |   if (options?.exact) {
> 40 |     expect.soft(actual).toEqual(expected);
     |                         ^ Error: expect(received).toEqual(expected) // deep equality
  41 |   } else {
  42 |     expect.soft(actual).toMatchObject(expected);
  43 |   }
  44 | }
  45 | 
```